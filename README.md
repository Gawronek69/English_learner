uni project for NLP course - simple webapp for helping with English learning - the syntactic part at least

Required : python, npm

How to run: 
1. run the `cd back` command
2. create virtual environment for the app or just run `pip install -r requirements.txt`
3. run the `cd app` command
4. run the `uvicorn main:app --reload`
